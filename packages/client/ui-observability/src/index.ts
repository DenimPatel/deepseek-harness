/** Pure host half; the whole dashboard lives in the browser export. */

/** Host plugin body: observability renders browser-only and contributes nothing to the host tree. */
export function apply(): void {}
